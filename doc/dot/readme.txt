The .dot files in this directory are processed by the "dot" program
that is part of Graphviz (http://graphviz.org/).

The easiest way to install Graphviz on MacOS is to use a MacPorts, or
a similar alternative open source package manager. It is also
available on Windows open source package managers. See:
http://graphviz.org/download/

